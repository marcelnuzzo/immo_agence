{% extends 'base_admin.html.twig' %}

{% block title %}Liste des catégories!{% endblock %}

{% block body %}

<div class = "jumbotron">
    <h1 style="text-align:center"><mark>Catégories</mark></h1>
    <a href="{{ path('home') }}" class="btn btn-info">Retour au menu principale</a> 
</div>
<a class="btn btn-warning" style="color:white" href="{{ path('admin_createCat') }}" role="button" >Créer</a>
<table class="table table-striped table-bordered table-hover">
    <tr>
        <th>Id</th>
        <th>Libellé</th>
    </tr>
    {%for categorie in categories %}
        <tr>
            <td>{{ categorie.id }}</td>
            <td>{{ categorie.libelle }}</td>
            <td><a class="btn btn-info" style="color:white" href="{{ path('admin_editCat', {'id':categorie.id}) }}" role="button" >Modifier</a></td>
            <td><a class="btn btn-danger" style="color:white" href="{{ path('admin_deleteCat', {'id':categorie.id}) }} " role="button" >Supprimer</a></td>
        </tr>
    {% endfor %}
</table>
{% endblock %}